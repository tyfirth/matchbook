<h1>Match Details</h1>

<h1> <%= link_to @match_game.name, game_path(@match_game) %> </h1>

<h1> <%= @match.title %> </h1>
<h3>Played On <%= @match.created_at.strftime(' %A,  %B %e %Y') %> </h3>

<br>

<h2>Winner: <%= link_to @match.winner, player_path(@match_winner) %> </h2><br>

<h3>Score: <%= @match.score %> </h3><br>

<h3>Players: </h3>
<ul>
  <% @match.players.each do |player| %>
    <li> <%= link_to player.name, match_player_path(@match, player) %></li>
  <% end %>
</ul>

<br><br>

<%= link_to "Edit Match Details", edit_match_path(@match) %>
<br><br>
